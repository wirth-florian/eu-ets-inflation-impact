# Replication package: " ... enter paper name here ..."

All code, data, and figures required to replicate the results are included.

## Data sources

- Note the data sources

## Requirements

Python (version 3.14.5 or higher) with Jupyter Notebook, with the following packages: 

All Python packages are available on PyPI and can be installed with:

```python
pip install -r requirements.txt
```

The mrio_toolbox package contains an update to handle the new Figaro tables, which is not yet available in the PyPI package. Therefore, it has to be locally installed via

```python
pip install -e mrio_toolbox
```


## How to replicate

Describe replication steps for the figures - Is this even needed?

## License

See which license we need (probably the same as the toolbox)
